[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "patent-mcp-server"
version = "0.12.0"
description = "Model Context Protocol server for USPTO patent data"
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = "MIT"
authors = [
    {name = "riemannzeta", email = "noreply@github.com"}
]
keywords = ["mcp", "model-context-protocol", "uspto", "patent", "patent-search", "api"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Legal Industry",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Information Analysis",
]

dependencies = [
    "httpx[http2]>=0.28.1",
    # <2 required: MCP SDK 2.0 removed mcp.server.fastmcp (renamed FastMCP
    # to MCPServer); migrating is a deliberate project, not a version bump.
    "mcp[cli]>=1.28.1,<2",
    "python-dotenv>=1.0.1",
    # Not imported by this package; security floor for a transitive dep of
    # mcp (CVE-2026-24486 fix; floor raised for PYSEC-2026-3036..3040).
    "python-multipart>=0.0.31",
    "tenacity>=8.0.0",
]

[project.urls]
Homepage = "https://github.com/riemannzeta/patent_mcp_server"
Repository = "https://github.com/riemannzeta/patent_mcp_server"
Issues = "https://github.com/riemannzeta/patent_mcp_server/issues"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["patent_mcp_server", "patent_mcp_server.uspto", "patent_mcp_server.util"]

[tool.setuptools.package-data]
patent_mcp_server = ["json/*.json"]

[dependency-groups]
dev = [
    "build>=1.3.0",
    "jaraco-context>=6.1.0", # CVE-2026-23949 fix
    "pip-audit>=2.10.0",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=7.0.0",
    "twine>=6.2.0",
    "urllib3>=2.6.3", # CVE-2026-21441, CVE-2025-66418, CVE-2025-66471 fix
]

[project.scripts]
patent-mcp-server = "patent_mcp_server.patents:main"
